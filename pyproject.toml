[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "malware-robustness"
version = "0.1.0"
description = "Reproducible evaluation of static malware classifier robustness."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "lightgbm>=4.5,<5",
    "numpy>=1.26,<3",
    "pandas>=2.2,<3",
    "pyarrow>=17",
    "pyyaml>=6.0,<7",
    "scikit-learn>=1.5,<2",
]

[project.optional-dependencies]
dev = ["pytest>=8.3,<9", "ruff>=0.6,<1"]
ember2024 = [
    "thrember @ git+https://github.com/FutureComputing4AI/EMBER2024.git@0ef753e81d98bf209f71b03cd331dfc190b5b54d",
]

[project.scripts]
malware-data = "malware_robustness.ember2024:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
