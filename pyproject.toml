[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "malware-robustness"
version = "0.1.0"
description = "Reproducible evaluation of static malware classifier robustness."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115,<1",
    "lightgbm>=4.5,<5",
    "numpy>=1.26,<3",
    "pandas>=2.2,<3",
    "pyarrow>=17,<26",
    "pyyaml>=6.0,<7",
    "scikit-learn>=1.5,<2",
    "uvicorn>=0.30,<1",
]

[project.optional-dependencies]
dev = ["httpx2>=2,<3", "pytest>=8.3,<9", "ruff>=0.6,<1"]

[project.scripts]
malware-data = "malware_robustness.ember2018:main"
malware-api = "malware_robustness.api.app:run"
malware-train = "malware_robustness.training:main"
malware-robustness = "malware_robustness.robustness_cli:main"
malware-harden = "malware_robustness.hardening_cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
